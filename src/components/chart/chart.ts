import { ChartArea } from './chart-area';
import { ChartPie } from './chart-pie';
import type { ChartObject } from './chart.types';

export const Chart: ChartObject = {
  Area: ChartArea,
  Pie: ChartPie,
};
